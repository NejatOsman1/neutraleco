# Create symlinks for assets
php bin/console assets:install --symlink --relative

# Update tables based on entities
php bin/console doctrine:schema:update --force

# Create new entity
php bin/console generate:doctrine:entity --no-interaction --entity=CmsBundle:Test --fields="title:string(100) body:text"

# Generate getters and setters
php bin/console doctrine:generate:entities CmsBundle/Entity/Test

# Generate getters and settings in whole bundle
php bin/console doctrine:generate:entities CmsBundle

# Create admin user
php bin/console fos:user:create admin admin@example.com password --super-admin






CREATE ENTITY RELATIONS

1) Add property and constructor entry to container entity:

	/**
	 * @ORM\OneToMany(targetEntity="Product", mappedBy="category")
	 */
	protected $products;

	public function __construct(){
		$this->products = new ArrayCollection();
	}

2) Add property to child entity:

    /**
     * @ORM\ManyToOne(targetEntity="Category", inversedBy="products")
     * @ORM\JoinColumn(name="category_id", referencedColumnName="id")
     */
    protected $category;

3) Fix missing setters and getters

	php bin/console doctrine:generate:entities AppBundle

4) Fix database connections between both tables

	php bin/console doctrine:schema:update --force

5) (optional) Add finder function to child repository

	public function findAllOrderedByName(){
        return $this->getEntityManager()
            ->createQuery(
                'SELECT p FROM AppBundle:Product p ORDER BY p.name ASC'
            )
            ->getResult();
    }