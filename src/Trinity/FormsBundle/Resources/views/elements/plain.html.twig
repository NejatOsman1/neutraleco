{% trans_default_domain 'forms' %}
{% extends [':forms_elements:row.html.twig', '@TrinityForms/elements/row.html.twig'] %}

{% block field %}
	<div class="plain-field" {% if edit is defined %}contenteditable="true" style="margin-top:40px;"{% endif %} data-type="title" style="outline:none;">{{Question.getTitle != ('Titel' | trans) ? (Question.getTitle|raw) : ('Vrije tekst' | trans)}}</div>

	{# {% if edit is defined %}<div class="block-id">Vrij #{{Question.id}}</div>{% endif %} #}
	{% if edit is defined %}
		<div class="info">
			<span class="qid">({{Question.getTypeLabel}}, ID: {{Question.id}})</span>
		</div>
	{% endif %}
{% endblock %}
